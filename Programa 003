Programa que apresenta os valores numéricos decimais, hexadecimais e octais de um caractere:

#include <stdio.h>
int main(){
    char x;
    scanf("%c",&x);
    printf("O caractere : %c tem os seguintes valores\n(decimal)\t>:%d \n(octal)         >:%d\n(hexadecimal)\t>:%d",x,x,(x-24),(x+36));
    return 0;
}
